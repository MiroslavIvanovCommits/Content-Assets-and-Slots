<h2 class="exclusive-products-title text-center w-100 mt-5">
    ${Resource.msg('products.exclusive.title', 'contentAssetsAndSlots', null)}
</h2>
<div class="row">
    <isloop items="${pdict.assetBody}" var="productId">
        <div class="col-sm-6 col-md-4">
            <isinclude
                url="${URLUtils.url('Tile-Show', 'pid', productId, 'pview', 'tile', 'ratings', false, 'swatches', false)}" />
        </div>
    </isloop>
</div>
